<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Mobile Verification using OTP</title>
    <% include partials/scripts.ejs %>
</head>
    <body onload="sendOTP()">
        <% include partials/navbar.ejs %>
        <p id="usertype" class="d-none"><%= usertype%></p>
        <p id="mobileverified" class="d-none"><%= mobileVerified %></p>
        <div class="container">
            <div class="text-center">
                <h1 class="display-4">Verify Your Mobile Number</h1>
            </div>               
            <!-- <form action="/otp" class="form-horizontal" method="POST"> -->
            <div>
                <div class="form-group">
                  <label for="mobileNumber" class="control-label label-font">Mobile Number</label>
                  <input readonly autofocus class="form-control input-font" id="mobile-number" type="text"placeholder="Your Mobile Number">
                  <small id="mobHelp" class="form-text text-muted">To change this number, go to Profile Page.</small>
                </div>
                <div class="form-group">
                  <label for="OTP" class="control-label label-font">Enter OTP sent to above Mobile Number.</label>
                  <input class="form-control input-font" name="otp" type="OTP" id="OTP" maxlength="4" minlength="4" placeholder="OTP">
                </div>
                <div class="form-group">
                    <button onclick="verifyOTP()" class="btn-rounded btn btn-primary">Verify OTP</button>
                </div>
                <div class="form-group">
                    <p id="timer"></p>
                    <small id="resendOtpCode" class="d-none">Didn't Receive an OTP ? <a onclick="sendOTP()" href="">Click to Resend</a></small>
                </div>
            <!-- </form> -->
            </div>
        </div>
    </body>
</html>
